@page

@model IndexModel
@{
    ViewData["Title"] = "Viettech Demo";
}

<div class="text-center">
    <h1 class="display-4">Customers Management</h1>
</div>

<div class="card">
    <div class="col-sm-12" style="padding:20px">
        <button onclick="jQueryModalGet('?handler=CustomerUpdate','Create Customer')" class="btn bg-success">
            Create
        </button>
        <button id="reload" class="btn bg-warning">
            Refresh
        </button>
    </div>
    <div id="customerList" class="card-body table-responsive"></div>
</div>